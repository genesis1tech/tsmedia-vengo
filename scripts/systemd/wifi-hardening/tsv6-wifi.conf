# TSV6 WiFi Driver Tuning for Broadcom brcmfmac
# Install to: /etc/modprobe.d/tsv6-wifi.conf
#
# Disables Broadcom internal roaming which conflicts with
# NetworkManager's own roaming logic, causing association failures.

options brcmfmac roamoff=1
